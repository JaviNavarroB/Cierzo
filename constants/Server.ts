// Ensure backend URL is correct and matches your server configuration
//export const SERVER_URL = 'http://localhost:3000/api';  // Asegúrate que este puerto coincida con tu backend
//import Constants from 'expo-constants';

//export const SERVER_URL = Constants.manifest?.extra?.serverUrl || '/api';
// config.ts
const SERVER_URL = '/api';
export default SERVER_URL;
